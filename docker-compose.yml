version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - qcm_data:/app/instance
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=production
      - SECRET_KEY=your_secret_key_change_me
    restart: unless-stopped

volumes:
  qcm_data:
